import intcode.permute
import java.math.BigDecimal
import java.util.*

val a = listOf(1,2,3)

a.filter {it != 2}

permute(listOf(0, 1, 2, 3, 4)).size

BigDecimal(4) * BigDecimal(2)

BigDecimal(2).toInt()


var queue: Queue<Int> = ArrayDeque<Int>(listOf(1, 2, 3))

queue

queue.remove()

queue.remove()

queue.isEmpty()

queue





